import type { Metadata } from "next";

export const metadata: Metadata = {
  title:
    "SSC CGL 2025 Tier 1 PYQ — Previous Year Questions with Answers",
  description:
    "Practice SSC CGL 2025 Tier 1 previous year questions with answers, explanations, timer and score analysis.",
};

export default function Layout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}